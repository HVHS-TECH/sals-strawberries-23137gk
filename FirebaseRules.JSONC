{
    // Allow anyone to read data
    // Only authenticated content
    // Owners can make changes to their data
    "rules": {
        ".read": true,
        ".write": false,
        "users": {
            "$uid": {
                ".read": true,
                ".write": "auth.uid === $uid"
            }
        }
    }
}